# Get a token at https://account.mapbox.com/access-tokens/
# Local dev:
VITE_MAPBOX_TOKEN=pk.your_mapbox_token_here
# Vercel (sensitive env var — no VITE_ prefix):
# MAPBOX_TOKEN=pk.your_mapbox_token_here
